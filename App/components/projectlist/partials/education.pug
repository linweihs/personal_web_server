h3.bordered
    strong Education
section(ng-controller="educationController")
    div(ng-repeat="i in educationList")
        h4
            a(href="{{i.url}}") {{i.name}}
            |
            |
            small [{{i.date}}]
        ul
            li {{i.degree}}
